#ifndef STDIO_H
#define STDIO_H
#include <stdio.h>
#endif

/**
 * @brief The input file, assumingly in .wojak language rules.
 * 
 */
FILE *input_file;

/**
 * @brief The symbol table file output
 * 
 */
FILE *sym_file;